<h1>Sales People</h1>

<div>
    <table>
        <thead>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Salary</th>
        </thead>
        {{!-- DELETE LATER --}}
        <tr>
            <td>John</td>
            <td>Doe</td>
            <td>50000</td>
            <td><a href="/customer_salesreps/{{id}}">Update</a></td>
            <td><button onclick="deleteProblem({{id}})">Delete</button></td>
        </tr>
    </table>

   

   
    {{#each salesperson}}
    <tr>
        <td>{{first_name}}</td>
        <td>{{last_name}}</td>
        <td>{{salary}}</td>
        <td><a href="/customer_salesreps/{{id}}">Update</a></td>
        <td><button onclick="deleteProblem({{id}})">Delete</button></td>
    </tr>
    {{/each}}
</div>