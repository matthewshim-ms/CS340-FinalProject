<h2>Update {{product.name}}</h2>
<form class="update-form" action="/products" method="POST">
  Name:<br>
  <input type="text" name="product_name" value="{{product.name}}">
  <br><br>
  Description:<br>
  <input type="text" name="description" value="{{product.description}}">
  <br><br>
  Quantity (between 1 and 99):
  <input type="number" name="quantity" min="1" max="99" value="{{product.quantity}}">
  <br><br>
  Price: (USD):
  <input type="number" name="price" min="1" step="any" value="{{product.price}}"/>
  <br><br>
</form>
{{!-- NOTE: Insert/Post will be triggered if button is within form --}}
<button onclick="updateProduct({{product.product_id}})">Update</button>