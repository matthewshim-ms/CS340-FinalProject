<h1>Customers</h1>

<div>
    <table>
        <thead>
            <th>First Name</th>
            <th>Last Name</th>
        </thead>
        {{!-- DELETE LATER --}}
        <tr>
            <td>Bill</td>
            <td>Jones</td>
            <td><a href="/customer/{{id}}">Update</a></td>
            <td><button onclick="deleteCustomer({{id}})">Delete</button></td>
        </tr>
        <tr>
            <td>Sally</td>
            <td>Smith</td>
            <td><a href="/customer/{{id}}">Update</a></td>
            <td><button onclick="deleteCustomer({{id}})">Delete</button></td>
        </tr>
    </table>

    {{#each customer}}
    <tr>
        <td>{{first_name}}</td>
        <td>{{last_name}}</td>
    </tr>
    {{/each}}    
</div>